apiVersion: v1
kind: Pod
metadata:
  name: abc-tech-pod
  annotations:
  kubernetes.io/config.mirror: "true"
spec:
  containers:
  - name: abc-tech-container
    image: kowshikreddy137/abc_tech:{{ build_number }}
    ports:
    - containerPort: 8080
