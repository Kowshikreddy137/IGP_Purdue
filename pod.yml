apiVersion: v1
kind: Pod
metadata:
  name: abc-tech-pod
  annotations:
spec:
  containers:
  - name: abc-tech-container
    image: kowshikreddy137/abc_tech:{{ BUILD_NUMBER }}
    ports:
    - containerPort: 8080
